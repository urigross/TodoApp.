import { InputBackclrDirective } from './input-backclr.directive';

describe('InputBackclrDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBackclrDirective();
    expect(directive).toBeTruthy();
  });
});
